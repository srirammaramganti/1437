#include "HeadSet.h"

int main(){
   //TODO: uncomment the following code after you complete the HeadSet class
   
   /*
   string brand;
   int maxVol, curVol, withMic, wired;
   cin >> brand >> maxVol >> curVol >> withMic >> wired;
   
   HeadSet set1(brand, maxVol, curVol, withMic, wired);
   set1.turnOnBlueTooth();
   set1.talk();
   set1.printConf(); 
   */
   
   HeadSet set;
   set.HeadSet();
   
   return 0;
}